using System;

namespace Library
{
    class Program
    {
        static void Main(string[] args)
        {
            var bookManager = new BookManager();
            bookManager.Manager();
        }
    }
}
